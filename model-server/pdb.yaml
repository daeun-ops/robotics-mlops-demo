apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: robotics-model-pdb, namespace: ? }
spec:
  minAvailable: 2           # 최소 2개 파드는 항상 살아있게
  selector: { matchLabels: { app: robotics-model } }
